# qa report — 2026-05-11

## verified

- branch: `overnight/2026-05-11-hardening`
- local command: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ./gradlew --no-daemon :app:assembleDebug :app:testDebugUnitTest --stacktrace`
- result: green
- pact check: three reminder permissions only, no network-adjacent permissions, no firebase/gms/analytics dependency

## work reviewed

- root pact link added to the README
- build workflow added
- iCalendar tests now cover leap-day all-day spans, weekly recurrence with `UNTIL`, folded descriptions, and unsupported field drop behavior
- iCalendar parsing now skips unknown nested components without consuming following events, and line folding uses UTF-8 byte length
- cold-start event/date launch routes take priority over the persisted default view, and exact-alarm denial is documented as an inexact fallback path

## risks

- unsupported iCalendar fields are intentionally lossy; the new tests document the drop but do not make complex recurrence compatible
- no emulator was reachable during the run, so reminder UI and notification runtime permission flows were not re-walked

## next improvements

- add repository tests around multi-event import and filename collision behavior
- add a small reminder scheduling test for recurring events after reboot reschedule
